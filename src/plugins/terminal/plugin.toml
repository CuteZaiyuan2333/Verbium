[plugin]
name = "terminal"
display_name = "Integrated Terminal"
version = "0.1.0"
author = "Verbium Agent"
description = "A real integrated terminal for Verbium."
dependencies = ["core"]

[external_dependencies]

portable-pty = "0.8"

vte = "0.11"

parking_lot = "0.12"

anyhow = "1.0"

unicode-width = "0.1"
