[plugin]
name = "code_editor"
display_name = "Code Editor Plugin"
version = "0.1.0"
author = "Verbium Team"
description = "A code editor with syntax highlighting (requires egui_extras)."
dependencies = ["core"]

[external_dependencies]
egui_extras = { version = "0.29.1", features = ["syntax_highlighting"] }
